# Captured images + per-group sidecars (user data, not code)
captures/
*.cr2

# Python
__pycache__/
*.pyc

# Backups / scratch
*.bak
scratchpad/

# Demo videos: too big for git history — the README embed is uploaded through
# GitHub's web editor instead (see README "The rig in action")
*.mp4
*.mov

# CI-staged SSH public key (injected from the SSH_AUTHORIZED_KEYS repo variable)
deploy/authorized_keys

# OS / editor
.DS_Store
*.swp
