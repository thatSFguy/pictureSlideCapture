# Captured images + per-group sidecars (user data, not code)
captures/
*.cr2

# Python
__pycache__/
*.pyc

# Backups / scratch
*.bak
scratchpad/

# CI-staged SSH public key (injected from the SSH_AUTHORIZED_KEYS repo variable)
deploy/authorized_keys

# OS / editor
.DS_Store
*.swp
